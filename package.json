{
  "name": "get-if-has-json",
  "version": "1.0.3",
  "description": "Extract JSON from a string that contains a bunch of crap before or after a valid JSON Object.",
  "keywords": [
    "JSON",
    "parse",
    "string",
    "crap"
  ],
  "homepage": "https://github.com/de-ar/get-if-has-json#readme",
  "bugs": {
    "url": "https://github.com/de-ar/get-if-has-json/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/de-ar/get-if-has-json.git"
  },
  "license": "WTFPL",
  "author": "Arslan Ahmad <de.arslanahmad@gmail.com>",
  "main": "lib/index.js",
  "types": "lib",
  "scripts": {
    "dev": "nodemon",
    "build": "run-s --print-label clean compile",
    "clean": "rimraf lib coverage .nyc_output",
    "compile": "tsc -p .",
    "pretty": "prettier -c . || prettier -w .",
    "pretest": "run-s build",
    "test": "jest --coverage",
    "pretest:coveralls": "run-s pretest",
    "test:coveralls": "jest --coverage --coverageReporters=text-lcov | coveralls"
  },
  "devDependencies": {
    "@types/node": "^14.14.41",
    "@types/nodemon": "^1.19.0",
    "@types/prettier": "^2.2.3",
    "@types/rimraf": "^3.0.0",
    "coveralls": "^3.1.0",
    "jest": "^26.6.3",
    "nodemon": "^2.0.7",
    "npm-run-all": "^4.1.5",
    "prettier": "^2.2.1",
    "rimraf": "^3.0.2",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4"
  }
}
